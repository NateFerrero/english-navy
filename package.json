{
  "name": "english-navy",
  "version": "0.1.0",
  "private": true,
  "description": "The English Navy — front-end-only vanilla ES module app (no build step, no dependencies).",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "@libsql/client": "^0.18.0"
  }
}
